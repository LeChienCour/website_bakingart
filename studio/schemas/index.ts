/**
 * Sanity schema registry.
 * Import and add schema type definitions here as they are created.
 *
 * Example:
 *   import { siteConfigType } from "./siteConfig";
 *   export const schemas = [siteConfigType];
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const schemas: any[] = [];
